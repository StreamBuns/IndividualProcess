本工程即StreamBuns 在Nwafu求学期间，负责的校重点项目CNVcaller改进的一部分。属于改进对于每个样品处理的流程。

使用方法：
编译命令
./src/make.sh

使用方法
运行命令实例
./IndividualProcess -b ../resources/ERR340328.bam -h ERR340328 -d ../resources/dupfile -s X -r ../resources/referenceDB.1000

-b bam文件 -h bam的标头 -d dupfile重复窗口记录文件 -s性染色体组成 -r窗口对照文件

需要注意
资源文件尽量新建一个resources文件放在下面，方便归类管理